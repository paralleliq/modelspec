# ModelSpec Validator (MVP)

Validates ModelSpec YAML/JSON files against the repository JSON Schema.

## Install (local)
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r tooling/validator/requirements.txt

